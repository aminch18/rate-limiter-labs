global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  # Gateway exposes /metrics in Prometheus text format (no client library needed).
  - job_name: 'gateway'
    static_configs:
      - targets: ['gateway:8080']
    metrics_path: /metrics
